name: app_build
description: Match reminder app for favorite football teams
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.0
  flutter_local_notifications: ^17.2.0
  timezone: ^0.9.2
  permission_handler: ^11.3.0
  workmanager: ^0.9.0+3
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
